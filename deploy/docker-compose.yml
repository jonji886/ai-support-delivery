services:
  api:
    build:
      context: ..
      dockerfile: apps/api/Dockerfile
    ports:
      - "8000:8000"
    environment:
      APP_ENV: ${APP_ENV:-demo}
      DEEPSEEK_ENABLED: ${DEEPSEEK_ENABLED:-true}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      WEB_PUBLIC_ORIGIN: ${WEB_PUBLIC_ORIGIN:-http://localhost:8080}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      OBSERVABILITY_DB_PATH: ${OBSERVABILITY_DB_PATH:-runtime/observability.db}
      OBSERVABILITY_INCLUDE_ERROR_MESSAGES: ${OBSERVABILITY_INCLUDE_ERROR_MESSAGES:-false}
      POLICY_MIN_EVIDENCE_SCORE: ${POLICY_MIN_EVIDENCE_SCORE:-0.65}
      POLICY_MIN_VECTOR_SCORE: ${POLICY_MIN_VECTOR_SCORE:-0.35}
      RAG_RETRIEVAL_STRATEGY: ${RAG_RETRIEVAL_STRATEGY:-fusion}
      CONVERSATION_TTL_HOURS: ${CONVERSATION_TTL_HOURS:-24}
      ORDER_SLOT_TTL_MINUTES: ${ORDER_SLOT_TTL_MINUTES:-1440}
      RETURN_REASON_SLOT_TTL_MINUTES: ${RETURN_REASON_SLOT_TTL_MINUTES:-60}
      INTENT_SLOT_TTL_MINUTES: ${INTENT_SLOT_TTL_MINUTES:-30}
      LOGISTICS_FACT_TTL_MINUTES: ${LOGISTICS_FACT_TTL_MINUTES:-5}
      RETURN_FACT_TTL_MINUTES: ${RETURN_FACT_TTL_MINUTES:-15}
      # Fault injection (disabled by default)
      MOCK_OMS_LATENCY_MS: ${MOCK_OMS_LATENCY_MS:-0}
      MOCK_OMS_FAILURE_RATE: ${MOCK_OMS_FAILURE_RATE:-0}
      MOCK_TICKET_LATENCY_MS: ${MOCK_TICKET_LATENCY_MS:-0}
      MOCK_TICKET_FAILURE_RATE: ${MOCK_TICKET_FAILURE_RATE:-0}
    volumes:
      - runtime-data:/app/runtime
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 10s
      timeout: 3s
      retries: 3
  web:
    image: nginx:1.27-alpine
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "8080:80"
    volumes:
      - ../apps/web:/usr/share/nginx/html:ro
    restart: unless-stopped

volumes:
  runtime-data:
